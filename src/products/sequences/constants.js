/**
 * Poll interval while a sequence is live: the list poll (while any sequence
 * is running) and the detail-page poll (while that one sequence is
 * running) both used this same number before this file existed.
 */
export const POLL_MS = 10000;
